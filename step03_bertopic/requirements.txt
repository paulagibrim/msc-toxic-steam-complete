# BERTopic Pipeline — Python Dependencies
# Install with: pip install -r requirements.txt
#
# Tested with Python 3.10+. GPU support requires a CUDA-enabled torch build;
# see https://pytorch.org/get-started/locally/ for the correct install command.

# Core modelling
bertopic>=0.16.0
sentence-transformers>=2.7.0
umap-learn>=0.5.6
hdbscan>=0.8.33

# Data processing
pandas>=2.0.0
numpy>=1.26.0
pyarrow>=14.0.0      # parquet read/write
scikit-learn>=1.4.0  # PCA, cosine similarity, preprocessing

# Parallelism for text cleaning
pandarallel>=1.6.5

# Text utilities
nltk>=3.8.0

# Deep learning backend (used by sentence-transformers and for seed control)
# For GPU: install the correct torch version from https://pytorch.org
torch>=2.1.0

# Hyperparameter optimisation
optuna>=3.6.0

# Topic coherence metrics
gensim>=4.3.0

# Experiment tracking
mlflow>=2.13.0

# Serialisation
joblib>=1.3.0        # PCA model persistence
safetensors>=0.4.0   # BERTopic model serialisation

# Hardware introspection
psutil>=5.9.0

# Configuration
pyyaml>=6.0.0

# LLM-assisted topic labeling (Stage 8)
anthropic>=0.40.0
tqdm>=4.65
